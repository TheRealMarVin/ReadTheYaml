description: HTTP server settings
fields:
  host:
    description: Host address to bind to
    required: true
    type: str
  port:
    description: Port number
    required: false
    default: 8080
    type: int
    range: [1, 65535]